import React from 'react';
import '../theme/ProfileHeader.scss'; // Use CSS module

const ProfileHeader: React.FC = () => {
  return (
    <header className='headerContainer'>
  </header>
  );
};

export default ProfileHeader;
